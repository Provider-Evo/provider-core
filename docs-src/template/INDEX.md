# template 索引

模板目录存放配置模板，版本更新时应与 `config.toml` 字段保持一致。

## 文件

- `template_config.toml` — Provider-V2 配置模板，用户分发基准。包含 server（含 `fast_restart`）、auth、gateway、proxy、platforms、fncall、debug、terminal 等配置段示例与注释。
