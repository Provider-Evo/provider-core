# src/webui 索引

- `app.py`：独立 WebUI 应用工厂。
- `page.py`：页面入口。
- `config_schema.py`：WebUI 配置与导出模型。
- `dependencies.py`：请求依赖与默认设置解析。
- `logs_ws.py`：WebSocket 日志广播器。
- `routes.py`：WebUI 路由注册入口。
- `server.py`：独立 WebUI 服务器与线程化服务器。
- `core/`：本地存储与异步任务管理。
- `middleware/`：WebUI 中间件。
- `routers/`：分路由处理器。
- `schemas/`：摘要序列化结构。
- `services/`：文档与摘要服务。
- `templates/`：布局与片段模板。
- `utils/`：导出等工具函数。
