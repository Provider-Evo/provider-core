[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "provider-fncall-util"
version = "1.0.0"
requires-python = ">=3.8"
dependencies = ["echotools>=2.3.7", "provider-sdk>=0.3.3"]

[tool.setuptools.packages.find]
include = ["provider_fncall_util*"]
