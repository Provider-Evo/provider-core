[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "provider-coplan-util"
version = "1.0.0"
requires-python = ">=3.8"
dependencies = ["provider-sdk>=0.3.3"]

[tool.setuptools.packages.find]
include = ["provider_coplan_util*"]
