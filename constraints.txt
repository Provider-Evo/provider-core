# Reproducible install constraints (mirrors pyproject.toml floors).
# Usage: pip install -c constraints.txt -e .

echotools>=2.3.0
provider-sdk>=0.3.2
aiohttp>=3.9.0
loguru>=0.7.0
tomlkit>=0.12.0
pydantic>=2.0.0
